export const metadata = {
  title: 'Reader Profile & Bookmarks',
  description: 'View your saved biographies, appreciated stories, and reading preferences.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProfileLayout({ children }) {
  return children;
}

