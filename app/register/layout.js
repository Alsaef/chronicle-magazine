export const metadata = {
  title: 'Create Reader Account',
  description: 'Join Chronicle Magazine to curate reading lists, bookmark historic profiles, and engage with extraordinary stories.',
  robots: {
    index: false,
    follow: true,
  },
};

export default function RegisterLayout({ children }) {
  return children;
}

